# Models

Canonical retained models:

- tuned Gradient Boosting for tabular pair-safe inference
- CNN1D for one-hot sequence inference
- BiGRU k-mer for tokenized sequence inference

Comparison-only classical models remain useful for publication tables but are not treated as production inference modes in the cleaned repository.

The transformer branch and older legacy models are preserved only in review or archive locations.

